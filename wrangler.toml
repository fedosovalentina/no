name = "no"
compatibility_date = "2024-09-01"
main = "src/worker.ts"

[assets]
directory = "./dist"
binding = "ASSETS"
run_worker_first = ["/api/*", "/favicon.ico", "/favicon.svg", "/favicon-*"]

# KV: Dashboard -> Workers & Pages -> no -> Settings -> Bindings
# Add KV namespace binding, variable name: COUNTER
